@{
    ViewData["Title"] = "Dashboard HR";
}

<h1>Dashboard - Recursos Humanos</h1>

<div class="stats-grid">
    <div class="stat-card">
        <div class="stat-value">@ViewBag.PendingCount</div>
        <div class="stat-label">Usuarios Pendientes</div>
    </div>
    <div class="stat-card">
        <div class="stat-value">@ViewBag.ActiveJobsCount</div>
        <div class="stat-label">Ofertas Activas</div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h3>Acciones Rápidas</h3>
    </div>
    <div class="card-body">
        <a href="@Url.Action("PendingUsers")" class="btn">Ver Usuarios Pendientes</a>
        <a href="@Url.Action("CreateJobOffer")" class="btn btn-success">Crear Nueva Oferta</a>
        <a href="@Url.Action("JobOffers")" class="btn btn-secondary">Ver Todas las Ofertas</a>
    </div>
</div>
